# Example at https://github.com/bwhite/picarus/blob/master/.travis.yml

language: cpp
compiler: gcc
branches:
  only:
    - master
notifications:
  recipients:
    - timothy.shields@live.com
  email:
    on_success: change
    on_failure: always
script: cmake -G "Visual Studio 11 Win64" -D CMAKE_INSTALL_PREFIX:STRING=./install --build ./build ./src
